# .agents

Agent configuration for this repo.

## Skills

Installed with `npx skills add`. Locked in `skills-lock.json` at repo root.

- frontend-design (anthropics/skills): use when building or reviewing
  public facing UI so output stays polished and intentional.
  Source: skills/frontend-design/SKILL.md inside .agents/skills.

## Notes

- Rerun `npx skills check` to check for skill updates.
- Keep project docs in docs/. This folder holds agent tooling only.
